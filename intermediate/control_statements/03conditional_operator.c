// condition ? if ture : if false;
#include<stdio.h>
void main()
{
    int a,b;
    printf("Enter two numbers :");
    scanf("%D%D",&a,&b);
    (a>b) ? printf("a is greater") : printf("b is greater");
}